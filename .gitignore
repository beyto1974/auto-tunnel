# build output
/auto-tunnel
/dist/

# runtime logs — these record the remote host, user, and container names
*.log

# coverage profiles (coverage.svg is committed on purpose, for the README badge)
coverage.out

# tooling
.tokensave/
